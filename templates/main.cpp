#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <cmath>
#include <stack>
#include <queue>
#include <vector>
#include <climit> //LLONG_MIN LLONG_MAX INT_MIN INT_MAX

#define MIN(x,y) ((x) < (y) ? (x) : (y)
#define MAX(x,y) ((x) > (y) ? (x) : (y)

using namespace std;

int main()
{


    return 0;
}
